version: 4.1.32
steps:
  - prompt: Locate the "Contact Us" link in the navigation menu.
    commands:
      - command: hover-text
        text: CONTACT US
        description: contact link in the navigation menu
        action: click
  - prompt: Click the "Contact Us" link.
    commands:
      - command: hover-text
        text: CONTACT US
        description: contact link in the navigation menu
        action: click
  - prompt: >-
      Assert that the contact page loads with the correct content.  # Use the
      content of the file from the environment
    commands:
      - command: assert
        expect: the contact page displays "Headquarters" and "Investors"
