version: 4.1.32
steps:
  - prompt: Identify the main call to action button on the homepage.
    commands:
      - command: hover-text
        text: Visit JD.com's main site
        description: main call to action button at the top of the page
        action: click
  - prompt: Click on the main call to action.
    commands:
      - command: hover-text
        text: 立即登录
        description: main call to action button on the right side of the page
        action: click
  - prompt: >-
      Assert that the expected page or section loads correctly.  # Use the
      content of the file from the environment
    commands:
      - command: assert
        expect: the login page with "密码登录" is displayed
